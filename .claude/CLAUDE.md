@../AGENTS.md

<!--
  A pointer, not a second copy. The instructions live in AGENTS.md at the
  repository root, in the cross-tool convention other agents already look for;
  Claude Code reads CLAUDE.md and not that name, so it imports it here. Two
  copies would drift, and the copy that drifted would be the one nobody was
  reading.

  Add Claude Code-specific instructions below the import if any are ever needed.
  Anything true of every agent belongs in AGENTS.md instead.
-->
